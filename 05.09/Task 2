Number 2

#include <iostream> 
#include <math.h>
using namespace std; 
int main(){ 
int a= 2; 
float b = 3.141; 
float y; 

y = pow(0.315*log10(a) / (1 + pow(0.711*log10(b)/(1+pow(log10(b/a),1./3)),1./3)),1./3);
cout <<y; 
return 0; 
}
